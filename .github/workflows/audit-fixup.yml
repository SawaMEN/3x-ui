name: Temporary audit fixup

on:
  push:
    branches:
      - audit/full-project-hardening-20260925

jobs:
  fixup:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity check
        run: echo audit-workflow-ok
